# Bridge Server Configuration

# Fossify Messages API (via WireGuard VPN)
FOSSIFY_API_URL=http://10.0.0.2:8080
FOSSIFY_AUTH_TOKEN=generate-with-openssl-rand-hex-32

# Bridge Security
BRIDGE_SECRET=generate-with-openssl-rand-hex-32

# Server Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Monitoring & Alerts
MONITOR_CHECK_INTERVAL=60
MONITOR_ALERT_COOLDOWN=300

# SMTP Configuration (for health monitoring alerts)
# Gmail example: smtp.gmail.com:587
# For Gmail, use an App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com
SMTP_TO=alert-recipient@example.com

# Note: VoIP.ms credentials are configured in mmsgate.conf, not here
# The bridge acts as a proxy - it intercepts calls to VoIP.ms API
# and routes them to Fossify instead
